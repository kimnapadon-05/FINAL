<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <title>พิมพ์สติ๊กเกอร์ครุภัณฑ์</title>
    <style>
        /* ====================
           1. การตั้งค่าการพิมพ์
           ==================== */
        @media print {
            .no-print { display: none; }
            body { margin: 0; }
        }
        
        /* ====================
           2. การตั้งค่าทั่วไป
           ==================== */
        body {
            font-family: Arial, sans-serif;
            text-align: center;
        }
        
        /* ====================
           3. กล่องสติ๊กเกอร์หลัก (.label)
           ==================== */
        .label {
            /* กำหนดขนาดที่ต้องการสำหรับสติ๊กเกอร์ */
            /* ปรับ width และ height ตามขนาดสติ๊กเกอร์จริงของคุณ */
            border: 1px solid #000;
            padding: 2mm; 
            width: 85mm;  /* ตัวอย่าง: กว้าง 75mm */
            height: 25mm; /* ตัวอย่าง: สูง 25mm */
            margin: 5mm auto; 
            border-radius: 3px;
            
            /* จัดเรียงองค์ประกอบให้เป็นแถวเดียวกัน */
            display: flex; 
            align-items: center; /* จัดให้อยู่กึ่งกลางแนวตั้ง */
            box-sizing: border-box;
            overflow: hidden; /* ป้องกันข้อความหรือรูปภาพล้น */
        }
        
        /* ====================
           4. QR Code (ซ้าย)
           ==================== */
        img.qr {
            width: 25mm; /* ขนาด QR Code */
            height: 25mm;
            margin-right: 5mm; /* เว้นช่องว่างระหว่าง QR กับข้อความ */
            /* ทำให้ QR code ไม่หดตัว */
            flex-shrink: 0; 
        }
        
        /* ====================
           5. ข้อมูลหลัก (ขวา)
           ==================== */
        .main-info-container {
            /* ภาชนะสำหรับข้อความทั้งหมด (รหัสครุภัณฑ์) */
            flex-grow: 1; /* ขยายให้เต็มพื้นที่ที่เหลือ */
            text-align: center; /* จัดให้อยู่กึ่งกลางของพื้นที่นี้ */
            line-height: 1; /* ลดระยะห่างบรรทัด */
            /* ตั้งค่าให้ข้อความไม่หลุดบรรทัด */
            word-wrap: break-word; 
        }
        
        .asset-id {
            /* รหัสครุภัณฑ์ให้ใหญ่ที่สุด */
            font-size: 18pt; 
            font-weight: bold; 
        }
        
    </style>
</head>
<body>
    <div class="no-print">
        <button onclick="window.print()">🖨️ พิมพ์สติ๊กเกอร์</button>
    </div>

    <div class="label">
        <img class="qr" src="admin/<?php echo $_GET['qrcode']; ?>" alt="QR Code Asset">

        <div class="main-info-container">
            <div class="asset-id"><?php echo $_GET['asset']; ?></div>
            </div>
    </div>
</body>
</html>